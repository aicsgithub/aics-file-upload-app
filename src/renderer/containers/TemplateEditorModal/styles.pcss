.infoAlert {
    margin-bottom: 1em;
}

.search {
    width: 100%;
}

.body {
    display: flex;
    height: 550px; /* todo find a way to make height more dynamic */
}

.list-container {
    flex: 1;
    display: flex;
    flex-direction: column;
    overflow-y: auto;
    height: 100%;
}

.list {
    border: var(--border);
    margin-right: 0.5em;
    overflow-y: auto;
}

.annotation-name, .tag {
    display: inline;
    margin-right: 1em;
}

.list, .form-container {
    flex: 1;
    padding: 0 1em;
}

.or {
    width: 100%;
    text-align: center;
    padding-bottom: 1em;
}

.form {
    border: var(--border);
}