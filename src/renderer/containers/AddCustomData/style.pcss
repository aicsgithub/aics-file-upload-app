.selectors {
  display: flex;
}

.schema-selector {
  flex: 2;
  z-index: 1;
}

.barcode {
  flex: 1;
  z-index: 1;
}

.alert {
  margin-bottom: 1em;
}

.spin-container {
  height: 100%;
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
}
