.button-row {
  display: flex;
  margin-bottom: 1em;
}

.delete-button {
  flex: 1;
}

.undo-redo-buttons {
  flex: 0 0 auto;
}

.row-highlight {
    background-color: var(--drag-and-drop-background-color);
    color: var(--brand-primary);
}
