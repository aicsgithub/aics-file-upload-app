.button-row {
  display: flex;
  margin-bottom: 1em;
}

.delete-button {
  flex: 1;
}

.undo-redo-buttons {
  flex: 0 0 auto;
}

.row-highlight {
    background-color: #e0eefc;
    color: var(--brand-primary);
}
