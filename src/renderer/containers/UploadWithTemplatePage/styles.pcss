.error-icon {
  color: var(--error);
}

.icon {
  padding-top: 5px;
  padding-right: 8px;
}

.alert {
  margin-bottom: 1em;
  max-height: 10em;
  overflow-y: auto;
}

/* TODO: Figure out styling */

.content-container {
  /* Flex-parent of data table */
  display: flex;
  flex-direction: column;

  height: calc(100% - 4em);
  overflow-y: auto;
  padding: 1em;
  user-select: auto;
}

.spin-container {
  height: 100%;
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
}

.help-text {
  color: lightgrey;
}

.separator-text {
  padding-top: 24px;
  margin: 0 10px;
}

.selector {
  width: 100%;
}
