.container {
  height: 100%;
  width: 100%;
  display: flex;
  flex-direction: column;

  & .title {
    flex: 0 0 auto;
    padding-top: 2em;
    text-transform: uppercase;
  }

  & .form-prompt {
    flex: 0 0 auto;
    font-size: 22px;
  }

  & .form {
    flex: 1;
    padding: 1em 0 4.5em 0;
  }

  & .loading {
    font-size: 24px;
    padding: 0 12px;
  }
}

.content {
  flex: 1;
  display: flex;
  flex-direction: column;
  width: 100%;
  height: 100%;
  overflow: auto;
}

.header {
  flex: 0 0 auto;
}

.buttons {
  flex: 0 0 auto;
  padding: 0.5em;
  display: flex;
  justify-content: center;
}

.back-button {
  flex: 0 0 auto;
}

.save-button {
  flex: 0 0 auto;
}
