.duration-editor-container {
    & :global(.ant-input) {
        height: 34px;
        padding: 4px;
    }

    & :global(.ant-input-group-addon) {
        font-size: 14px;
        padding: 0 2px;
        border-radius: 0;
    }
}