.container {
  display: flex;

  &.read-only {
    flex: 1;
    height: 100%;
    display: flex;
    align-items: center;

    &:hover {
      cursor: pointer;
    }

    & div {
      padding-left: 0.3em;
      flex: 1;
    }
  }

  & .input:global(.ant-input) {
    min-height: inherit;
    opacity: 1;
    flex: 1;
    border: none;

    &:focus, &:hover {
      border: none;
      box-shadow: none;
    }
  }
}
