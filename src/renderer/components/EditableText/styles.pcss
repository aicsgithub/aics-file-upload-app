.read-only {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;

  &:hover {
    cursor: pointer;
  }

  & span {
    flex: 1;
  }
}

.input:global(.ant-input) {
  width: 100%;
  opacity: 1;
  background-color: transparent;
}
