.icon {
  font-size: 30px;
}

.warn {
  /* Variable found in styles/colors.pcss */
  color: var(--warning);
}

.success {
  /* Variable found in styles/colors.pcss */
  color: var(--success);
}

.error {
  /* Variable found in styles/colors.pcss */
  color: var(--error);
}

.info {
  /* Variable found in styles/colors.pcss */
  color: var(--info);
}

.save {
  color: #003057;
}
